# Spring Boot API base URL. Keep /api at the end.
VITE_API_BASE_URL=http://localhost:8080/api

# Spring Security starts Google sign-in from this backend endpoint.
# Google client ID and client secret stay in the backend configuration, never here.
VITE_OAUTH_LOGIN_URL=http://localhost:8080/oauth2/authorization/google
