package com.skillsphere.domain;

/** Types map directly to the notification examples in the requirements. */
public enum NotificationType {
    COLLABORATION_REQUEST,
    COMMUNITY_JOINED,
    PROFILE_VERIFIED,
    PROJECT_REQUEST_ACCEPTED,
    ADMIN_ANNOUNCEMENT,
    GENERAL
}
